import Modal from "../UI/Modal";

const Order = (props) => {

    return(
        <Modal>
            <div>
                <p style={{color: 'green'}}>Order placed Succefully!!!</p>
            </div>
        </Modal>
    );
}

export default Order;